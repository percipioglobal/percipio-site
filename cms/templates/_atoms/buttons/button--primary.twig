{% extends "_atoms/buttons/button--variables" %}

{% block button %}

    {%- minify -%}

        <a {{ values.url ? 'href=' ~ values.url : '' }} class="inline-flex border px-8 py-4 font-bold text-lg border-{{- values.swatch.primary }} text-{{- values.swatch.primary }} hover:text-white-100 hover:bg-{{- values.swatch.primary }} {{ values.utilities ? values.utilities : '' -}}">
            {{- values.text -}}
        </a>

    {%- endminify -%}

{% endblock %}

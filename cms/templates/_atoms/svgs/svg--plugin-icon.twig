{#
    ### PROPERTIES ###
    ------------------

    icon: (Asset) - the SVG asset
    utlities: (STR) - the icon utilities

    ### FUNCTIONS ###
    -----------------
    ga: parse google analytics attributes
#}

{%- set props = {
    icon: options.icon ??? null,
    utilities: options.utilities ??? null,
} -%}

{%- if props.icon and props.icon.extension == 'svg' -%}
    <div class="absolute top-0 left-1/2 -translate-x-1/2 -translate-y-1/2">
        {{- svg(props.icon, sanitize=false, namespace=false)|attr({ class: props.utilities }) -}}
    </div>
{%- endif -%}
{% extends "_atoms/chips/chip--variables" %}

{% block chip %}

    {%- minify -%}

        <span class="inline-block mb-2 px-4 py-1 font-mono lowercase text-sm rounded-full {{ values.inverted ? 'text-white-100 bg-' : 'bg-white-100 text-' -}}{{- values.colors.primary }} {{ values.utilities ? values.utilities : '' -}}">
            {{- values.text -}}
        </span>

    {%- endminify -%}

{% endblock %}

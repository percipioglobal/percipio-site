{# fetch an icon from the Font Awesome library #}
{%- macro fa_icon_lut(type) -%}
	{%- set lut = {
        'angle-right': 'far fa-angle-right',
	} -%}
	{%- apply spaceless -%}
		{{- lut[type] ??? 'far fa-hashtag' -}}
	{%- endapply -%}
{%- endmacro -%}

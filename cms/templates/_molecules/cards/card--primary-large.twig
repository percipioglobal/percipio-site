{% import '_macros/atoms' as render %}

<div class="col-span-2 relative bg-white shadow-2xl rounded-4xl p-6 pb-9 overflow-hidden">

    <div class="relative rounded-[20px] overflow-hidden">
        {{- render.atom('image', 'card-large', {
                image: content.cardImage.collect().first(),
                sizes: ['(max-width:768px) 100vw', '(min-width:768px) 66.6vw'],
                alt: content.title,
                placeholders: placeholders,
            })
        -}}
    </div>

    <div class="space-y-4 mt-8">
        <h3>
            {{- render.atom('text', 'card-title', {
                content: content.title ??? ''
            }) -}}
        </h3>

        {%- if content.subtitle ??? null -%}
            <h4>
                {{- render.atom('text', 'card-subtitle', {
                    content: content.subtitle
                }) -}}
            </h4>
        {%- endif -%}
    </div>

    <div class="bg-pink-500 h-3 absolute bottom-0 left-0 w-full"></div>
</div>

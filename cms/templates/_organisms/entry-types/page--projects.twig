{%- import '_macros/atoms' as render -%}

{%- set projects = craft.entries.section('projects')
    .orderBy('postDate desc')
    .all() -%}

{%- do craft.eagerBeaver.eagerLoadElements(entry, [
        'heroImage'
    ])
-%}

{%- include '_organisms/heroes/hero--landing' -%}

<div id="content" class="container max-w-screen-hd px-8 mx-auto">
    {%- include '_organisms/cards/cards--highlight' ignore missing with {
        content: projects,
        type: 'projects',
        hasFeatured: true,
    } -%}
</div>
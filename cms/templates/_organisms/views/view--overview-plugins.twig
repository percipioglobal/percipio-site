{%- set plugins =
    craft.entries
    .section('plugins')
    .with([
        'pluginIcon'
    ])
    .collect()
-%}

{%- block content -%}
    {%- include '_organisms/builders/builder--hero' ignore missing with {
        content: entry,
    } -%}

    {%- include '_organisms/grids/grid--plugins' with {
        plugins: plugins,
    } -%}
{%- endblock -%}

{%- set entries =
    craft.entries
    .section('blog')
    .with([
        'cardImage'
    ])
    .collect()
-%}

{%- block content -%}
    {%- include '_organisms/builders/builder--hero' ignore missing with {
        content: entry,
    } -%}

    {%- include '_organisms/grids/grid--blogs' ignore missing with {
        cards: entries,
        utilities: 'md:mt-20'
    } -%}
{%- endblock -%}

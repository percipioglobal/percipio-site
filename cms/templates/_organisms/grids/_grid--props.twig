{#
    ### PROPERTIES ###
    ------------------

    firstCard: (Collection) -- The first item of the collection
    chunks: (Collection) -- The collection divided in chunks
    startSize: (ENUM) -- The card size that goes first: large|small
#}

{%- set props = {
    firstCard: cards.first() ??? null,
    chunks: cards.slice(1).chunk(2) ??? null,
    startSize: startSize ??? 'large',
} -%}

{%- block grid -%}
{%- endblock -%}
<div class="container mx-auto max-w-screen-2xl">

    <div class="flex flex-wrap -mx-{{- spacing }} mt-0 md:mt-16 mb-10 md:mb-32">

        {% for item in grid.items %}

            {%- include '_molecules/griditems/griditem--content' ignore missing with { 
                content: item,
                spacing: spacing,
                prose: prose,
            } -%}

        {% endfor %}

    </div>

</div>